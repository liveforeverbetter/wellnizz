node_modules/
dist/
.env
.env.*
!.env.example
.secrets/
npm-debug.log*
data/lab-locations/*.json

# Internal-only material: runbooks, infra notes, plans. Never publish.
docs-internal/

# Real Fly deploy config carries the specific app name; keep it local.
fly.toml
!fly.toml.example

.gstack/
.playwright-mcp/
/docs-internal/
content/out/

# skill-creator eval workspace (local only)
.skill-workspace/
.prs-reference/

# Generated reference-allele cache for the PGS scorer (one-time build from dbSNP)
data/genetics/pgs/reference-alleles-*.json

# Local session snapshots (ephemeral)
.foreverbetter-session/
